import { Card, CardContent, Typography } from '@mui/material'

export default function Operations() {
  return (
    <Card>
      <CardContent>
        <Typography variant="h6">Operations</Typography>
        <Typography variant="body2">Purchase Orders, Sales Orders, Suppliers, Customers (đang phát triển)</Typography>
      </CardContent>
    </Card>
  )
}
